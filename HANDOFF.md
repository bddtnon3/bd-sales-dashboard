# HANDOFF.md — BD Sales Dashboard (สรุปส่งงาน)

เอกสารส่งต่องาน: สิ่งที่ตกลงกันไว้, ประวัติการแก้ไข, และข้อควรระวัง
คู่กับ `CLAUDE.md` (คู่มือเชิงเทคนิคสำหรับ Claude Code) — ไฟล์นี้เก็บ "เหตุผลเบื้องหลัง"

---

## 1. ระบบนี้ทำอะไร

เว็บแอปภายในของ **BD Distribution** (ตัวแทนจำหน่าย Unilever) แทนการส่งรูปรายงานในกลุ่ม LINE
แอดมิน/ผู้จัดการอัพไฟล์ Excel ผ่านหน้าเว็บ → ทีมเซลล์เห็นข้อมูลสดพร้อมกันทุกคน

**Live:** https://bd-sales-dashboard.vercel.app
**Repo:** `bddtnon3/bd-sales-dashboard` (deploy = push ขึ้น `main` → Vercel ~1 นาที)

**แท็บในระบบ**

| แท็บ | ใช้ทำอะไร |
|---|---|
| ภาพรวมยอดขาย | ยอดจริง vs เป้า รายวัน/เดือน · กดชื่อเซลล์เพื่อเจาะรายร้าน (วัน/สัปดาห์/เดือน/ไตรมาส/ปี + %เทียบงวดก่อน) |
| KPI | Active/Self/Ghost + เงินรางวัล |
| การสั่งของ | สถานะใบสั่ง/Drop · สรุปคำขอเซลล์แบ่งตาม 6 หมวดใบสั่งซื้อ · ค้นประวัติสินค้ารายตัว |
| สต็อก | สต็อกจริง/รอตัดบิล/เปิดใบสั่งซื้อ · คอลัมน์ต้นทุนเห็นเฉพาะ manager |
| คำขอสินค้า | เซลล์คีย์คำขอ → manager สรุปเปิดใบสั่ง → เซลล์เห็นสถานะได้/ไม่ได้ของ |
| วิเคราะห์เชิงลึก | สัดส่วนสินค้า (กลุ่ม/แบรนด์/ร้าน) · Top 20 มาแรง · เทียบช่วงเวลา · A/B เทียบสายกับค่าเฉลี่ยทีม |

**สิทธิ์:** `manager` เห็นทุกสาย + ต้นทุน + มูลค่า CON Confirm · เซลล์เห็นสายตัวเอง (ไม่เห็นต้นทุน)

---

## 2. ⚠️ ข้อควรระวังสำคัญที่สุด — ข้อมูลเก่าต้องไม่หาย

**เคยเกิดขึ้นจริง:** ข้อมูลยอดรายวัน เป้าเดือน 7 สต็อก และใบสั่งของ **หายทั้งหมด**
สาเหตุ: การเซฟทับทั้งก้อน + ลบ blob เก่าจนหมด กู้คืนได้บางส่วนจาก snapshot
(ของวันที่ 7–9 ก.ค. กู้ไม่ได้เลย)

**สิ่งที่วางไว้กันไม่ให้เกิดซ้ำ — ห้ามรื้อ:**

1. **`mergeState` ใน `api/save.js`** — เซิร์ฟเวอร์ *merge* ข้อมูลใหม่เข้าไปในของเดิมทีละ key
   ไม่ได้เขียนทับทั้งก้อน → อัพส่วนหนึ่งไม่ลบส่วนอื่น และเบราว์เซอร์ที่ข้อมูลเก่าค้าง
   ไม่สามารถย้อนข้อมูลใหม่ของคนอื่นได้
2. **`mergeRequests`** — manager เซฟทับคำขอของเซลล์ไม่ได้
3. **กันเซฟข้อมูลว่าง** — payload ที่ไม่มีข้อมูลเลยจะถูกปฏิเสธ (409)
4. **เก็บ backup 8 ชุด** (`KEEP=8`) — เซฟพลาดครั้งเดียวไม่ล้างประวัติ
5. **`api/data.js` อ่าน snapshot ใหม่สุดที่ "ไม่ว่าง"** — ถ้าตัวใหม่สุดว่าง จะถอยไปตัวก่อนอัตโนมัติ

> ถ้าจะแก้ตรรกะ merge: ต้องพิสูจน์ว่าข้อมูลเก่ายังอยู่ครบก่อน push ทุกครั้ง

**อีกเรื่อง — ขนาด payload:** Vercel จำกัด request ~4.5MB แต่ข้อมูลหลายเมกะไบต์
ฝั่งเบราว์เซอร์จึง gzip ก่อนส่ง (header `x-body-gzip:1`) **ห้ามใส่ `Content-Encoding: gzip`**
(edge จะแตกไฟล์ให้ก่อน แล้วเซิร์ฟเวอร์อ่านซ้ำ = พัง) เคยเจอ error `HTTP 413` จากเรื่องนี้

---

## 3. วิธีแก้โค้ด (สำคัญ — พลาดกันบ่อย)

**ต้นฉบับคือ `dashboard_template.html` ไม่ใช่ `public/index.html`**

```
แก้ dashboard_template.html → node build.cjs → commit ทั้ง 2 ไฟล์ → push main
```

`public/index.html` เป็นไฟล์ที่ build ออกมา **ห้ามแก้มือ** (จะถูกทับรอบถัดไป)
ถ้า build ขึ้น `ANCHOR NOT FOUND` = แก้โดนข้อความที่ build ใช้เป็นจุดอ้างอิง ต้องไปแก้ `build.cjs` ให้ตรงกัน
(หมายเหตุ: `package.json` เป็น `type: module` เลยต้องเป็น `.cjs`)

---

## 4. การตั้งชื่อไฟล์ที่อัพ (วันที่อยู่ในชื่อไฟล์)

| ประเภท | ตัวอย่างชื่อ |
|---|---|
| ใบสั่งซื้อ | `BD_250726.xlsx` · รอบเพิ่ม `BD_250726 (add1).xlsx` |
| ใบ Drop | `Drop 25072026.xlsx` |
| สต็อก / ยอดรายวัน / เดือน / master / รายร้าน | มีวันที่หรือเดือนในชื่อไฟล์ |
| KPI eB2B & Self Ordering | ระบบอ่าน "as of …" ในหัวรายงานเองก่อน ถ้าไม่มีก็ใช้วันที่ในชื่อไฟล์ ถ้าไม่มีอีกก็ใช้วันที่อัพ |
| DCI Score ศูนย์ | `R1_GBKK__EAST_DCI_Score_2026_BD_DISTRIBUTION.xlsm` (**ต้องเอารหัสผ่านออกก่อน**) |

ระบบเก็บข้อมูลตามวัน/เดือน/สาย → อัพซ้ำวันเดิมคือ *แทนของวันนั้น* ไม่กระทบวันอื่น

---

## 5. ตรรกะธุรกิจที่ฝังไว้ (จุดที่เข้าใจผิดง่าย)

- **Drop O/P/Q** — `O` = สั่งไป, `P` = ได้ของ (อาจบางส่วน, 0 = ไม่ได้เลย), `Q` = ดรอปจริง (O−P)
  แถบแดงขึ้นเมื่อไม่ได้ของเลย
- **มูลค่า Drop** อ่านจากคอลัมน์ **"Drop Value"** (จับด้วยชื่อหัวคอลัมน์ ไม่ fix ตำแหน่ง เพราะคอลัมน์ขยับได้)
- **การ์ด CON Confirm** (เห็นเฉพาะ manager) แสดง 4 บรรทัด:
  `Con Confirm` → `Con Confirm −15%` → `Drop` → `(Con Confirm −15%) − Drop`
  (−15% คือประมาณการตามที่ตกลง · ยอด Drop ใช้ตรงจากไฟล์ ไม่หักลบ)
- **ไฟล์ Drop มีสินค้ารหัสซ้ำหลายบรรทัด** (แยกตาม PO) — dashboard รวมยอดตามรหัสให้
  จึงมีปุ่มสลับ **📊 สรุปยอดรวม / 📄 แบบ Excel** เพื่อไล่เทียบกับไฟล์ต้นทางได้
  *(เคยเข้าใจว่าสินค้าหาย จริงๆ คือถูกรวมยอด — ตรวจแล้วยอดครบ)*
- **รหัสสินค้าเปลี่ยนบ่อย** — ระบบจับคู่ด้วย **ชื่อสินค้า** สำรอง (`normProd`) เช่น N-BVI
  ทำให้ของที่สั่งด้วยรหัสเก่า/ใหม่ยังนับเป็นตัวเดียวกัน แสดง "(รหัสใหม่ XXX)"
- **หมวดในใบสั่งซื้อ 6 หมวด** (Home_Care / PC_Hair / PC_3D / Skin_Care1 / Skin_Care2 / Best_F1)
  ระบบจับจาก **ชื่อชีต** ในไฟล์ใบสั่ง แล้วผูกทั้ง **รหัสและชื่อ** เผื่อรหัสเปลี่ยน
  ตารางที่ manager ต้องไล่เทียบกับไฟล์ Confirm ทีละหน้า จะ**แบ่งเป็นหัวข้อตามหมวด**เรียงลำดับเดียวกับ
  ใบสั่ง — ทั้งหน้า **📥 คำขอสินค้าจากเซลล์** และทุกกลุ่มใน **📊 วิเคราะห์การสั่งของ → สถานะสินค้า**
  (`psTable`) · สินค้าที่ไม่รู้หมวดตกไปอยู่ 📦 อื่นๆ · ถ้ามีหมวดเดียวจะไม่ขึ้นหัวข้อให้รกตา
- **ติดตามข้ามรอบ** — เซลล์สั่งวันที่ 15 ศูนย์อาจสั่งวันที่ 16 ไม่ได้ของ แล้ววันที่ 17 ได้
  สถานะจะไล่ไปข้างหน้าและบอกว่าได้ของวันไหน (มีสถานะ "🕓 สั่งแล้ว รอผล")
- **รอบสั่งตัดที่ 9 โมง** (`orderRoundDate`)

### DCI Score ของศูนย์ (แท็บ 📈 ทุกคนเห็น · อัพไฟล์ได้เฉพาะ manager)
อ่านกติกามาจาก **สูตรจริง** ในไฟล์ DCI Score 2026 ของ Unilever — อย่าเดาเอง ถ้าไฟล์ปีหน้าเปลี่ยน
ต้องเปิดดูสูตรใหม่แล้วแก้ `DCI_W` / `DCI_FULL` / `DCI_ZERO` ใน `dashboard_template.html`

- **100 = "ทำได้ตามเป้า" ไม่ใช่เพดาน** — น้ำหนักเต็มของ 8 ข้อรวมกันได้ 100 พอดีทุกช่วง
  บางข้อทำเกินเป้าแล้วได้คะแนนเกินน้ำหนัก (ข้อ 3, 4 ได้ถึง 12 จากน้ำหนัก 10 ใน Q2)
- **น้ำหนักเปลี่ยนตามช่วง** — Q1 `[50,10,5,5,5,5,10,10]` · **Q2 ตัดข้อ 2,5,6 เหลือ 0 แล้วโยกไป
  ข้อ 3,4,7,8** → `[50,0,10,10,0,0,15,15]` · H2 (BM7+) `[50,10,5,5,0,10,10,10]`
- **ในไฟล์ไม่มีคะแนนติดลบเลย ต่ำสุดคือ 0** — หน้าเว็บจึงแสดง "เก็บได้เท่าไหร่ / ขาดอีกเท่าไหร่"
  ห้ามแสดงเลขติดลบ (เจ้าของงงมาแล้วครั้งหนึ่ง) มีเทสต์คุมไว้ว่าไม่มี `-\d` โผล่บนหน้าจอ
- **หน้าผา (ตกเป็น 0 ทันที ไม่ค่อยๆ ลด)** — Sales Growth ต่ำกว่า **95%** ของเป้า = 0 (เสีย ~50 คะแนน) ·
  Active eB2B ต่ำกว่า **40%** = 0 · Self ordering ต่ำกว่า **25%** = 0 ·
  **% Coverage ต่ำกว่า 49.5% = คะแนนรวมทั้งเดือนเป็น 0** (แถว 53 ในไฟล์)
- **เส้นที่ต้องข้ามถึงได้คะแนนเต็ม** — Active eB2B **45%** · Self ordering **30%** (เท่ากันทุกช่วง)
- **ข้อ 7 Combo Drive / ข้อ 8 Flash Deal เป็นผ่าน/ไม่ผ่านล้วนๆ** ทำได้ 176% กับ 100.1% ได้คะแนน
  เท่ากันเป๊ะ — นี่คือเหตุผลที่ "ทำงานดีแต่คะแนนไม่ขึ้น"
- **ช่องไตรมาสในไฟล์เป็นค่าเฉลี่ย/ผลรวมของ 3 เดือน** ถ้าเดือนยังไม่ครบเลขจะต่ำหลอกตา
  (Q3 ที่มีแค่ BM7 ขึ้น 70 กว่าๆ ทั้งที่ BM7 จริงได้ 106) → `dciExtract` จึง**ซ่อนไตรมาสที่ยังไม่ครบ 3 เดือน**
- **ไฟล์ที่ Unilever ส่งมาใส่รหัสผ่านไว้** SheetJS เปิดไม่ได้ → ต้องเปิดแล้ว Save As เป็นไฟล์
  ไม่ใส่รหัสก่อนอัพ · ระหว่างที่ยังไม่อัพ หน้าเว็บใช้ `DCI_SEED` (BM1–BM7 อ่านจากไฟล์จริง,
  **อ่านอย่างเดียว ไม่เคยเขียนกลับ**)
- **ฝั่งเซลล์เห็นแท็บนี้ด้วย** (เจ้าของสั่งให้เปิดให้ทีมเซลล์ดู เพราะตัวฉุดหลักคือ Active eB2B
  ซึ่งทีมเซลล์เป็นคนดันเอง) — ปุ่มอัพโหลดยังเป็น `.admin-upload` เห็นเฉพาะ manager
  และข้อความบนหน้าจอไม่บอกให้เซลล์ "อัพไฟล์" เพราะเขาอัพไม่ได้

---

### การสั่งของ: แท็บย่อย 4 อัน + คำนวณ PO อัตโนมัติ
`#orderView` แบ่งเป็น 4 กลุ่มด้วย `ordSetSec()` — คำขอเซลล์ / ใบสั่งของ / ใบ Drop / คำนวณ PO
เป็นการซ่อน-แสดงกล่องล้วนๆ ไม่แตะ `switchTab`/`CURTAB`/`openUpload` และไม่แตะ anchor ของ `build.cjs`

- **`renderOrder` ต้องเติมข้อมูลทุกกล่องทุกครั้ง แม้กล่องนั้นถูกซ่อน** — parser เรียก `renderOrder()`
  แล้วผู้ใช้สลับแท็บทีหลัง ถ้า "ปรับให้เร็วขึ้น" โดยข้ามกล่องที่ซ่อน จะเห็นข้อมูลเก่าค้าง
- **`#orderSearchResult` ต้องอยู่นอกทุกแท็บย่อย** — `renderOrderSearch` คุม `.hidden` ของตัวมันเอง
  ถ้าเอาไปไว้ในแท็บที่ปิดอยู่ ค้นหาแล้วจะไม่เห็นอะไรเลย
- **ปุ่มแท็บย่อยต้องเป็น `<button>` จริง** ไม่ใช่ `<div>` — ตัวจับคลิกเพื่อคัดลอก (ชั้น capture)
  ข้าม `a,button,input,select,textarea,option` ถ้าใช้ div คลิกจะถูกกินไปเป็น "คัดลอก"
- กล่อง "ไฟล์ที่อัพ" มี **สองกล่อง** (`#orderManage` ใบสั่ง, `#orderManageDrop` ใบ Drop) เพราะแท็บแยกกัน
  `.admin-only` เป็นคลาสตกค้างไม่มี CSS ต้องคุมด้วย `style.display` เองทั้งสองกล่อง

**เครื่องคำนวณ PO (`poBuild`) อ่านอย่างเดียว ห้ามเขียน ORDERS / POSTATUS / STOCKD / REQUESTS เด็ดขาด**
(merge ของสามก้อนแรกเป็นแบบทับทั้ง key และ `mergeRequests` ให้ `at` ใหม่ชนะ — ถ้าเครื่องคำนวณไป
"เติมข้อมูล" ใส่ จะเป็นทางทำข้อมูลการสั่งจริงหายโดยไม่มี tombstone กู้) จำนวนที่แก้มืออยู่ใน `PO.edit`
ในหน่วยความจำรอบนั้นเท่านั้น มีเทสต์คุมว่าเรียกทั้งหมดแล้วสี่ก้อนนั้นไม่เปลี่ยนแม้ไบต์เดียว

- ฐานการสั่ง 3 แบบ ติ๊กแยกหรือรวมกันได้ → **เอาค่ามากที่สุดของฐานที่ติ๊ก** (ไม่ใช่บวกกัน)
  แล้วปัดขึ้นตามโปรฯ (`poPromoRound` — **ปัดขึ้นเท่านั้น**) แล้วตัดด้วยโควต้า
- คอลัมน์ **ที่มา** แสดงสูตรจริงทีละขั้น (`poWhyQty`) — เจ้าของต้องตรวจได้ว่าเลขมาจากไหน ห้ามซ่อน
- `s` (คาดเทา) / `x` (ปิดช่อง) = ห้ามสั่ง · `q` = เพดาน ไม่ใช่บล็อค · รหัสเปลี่ยนใช้ `poSwapFor(day,code)`
  (**ไม่ใช่ `psSwap`** ซึ่งอ่าน `PS2.date` ของอีกแท็บ)

### ตารางแบ่งกลุ่มต้องเป็น "ตารางเดียวไล่ลงมา" (`grpTable`)
เดิมทำเป็นตารางแยก กลุ่มละหนึ่งตาราง — เบราว์เซอร์คิดความกว้างคอลัมน์ของแต่ละตาราง
**แยกกันจากเนื้อหาในตารางนั้น** คอลัมน์จึงไม่ตรงกันระหว่างกลุ่ม อ่านแล้วลายตา
(เจ้าของแจ้ง 18 ก.ย. 69: "มีเบี้ยว อ่านแล้วปวดหัวลายตา")

**ห้ามสร้างตารางแยกต่อกลุ่มอีก** ใช้ `grpTable(head,groups,opt)` — ตารางเดียว หัวตารางครั้งเดียว
คั่นกลุ่มด้วยแถว `.grp-row` (`colspan` เต็มความกว้าง) คอลัมน์จึงตรงกันโดยธรรมชาติ
มี `grpByCat(rows,codeOf)` ช่วยแบ่งตามหมวดในใบสั่งซื้อเรียงลำดับเดิม
ที่แปลงแล้ว: คำขอสินค้าจากเซลล์ · สถานะสินค้า (`psTable`) · PO ที่สั่งได้ (`poCanTable`) ·
PO ที่สั่งไม่ได้ (`poCantTable`)

- **แถบคั่นกลุ่มต้องไม่ถูกตัวคัดลอกจับ** — `cpFind` ดูที่ `<td>` ที่เป็นตัวเลข 6–9 หลัก
  และช่องถัดจากช่องรหัส แถบคั่นเป็น `<td colspan>` ที่มีข้อความไทย จึงรอดอยู่แล้ว มีเทสต์คุม
- **การ์ดรายเซลล์** (`.req-perwrap` เรียงข้างกัน/เรียงลงมาบนมือถือ) ยังเป็นตารางแยกใบตามดีไซน์
  แต่บังคับ `table-layout:fixed` + สัดส่วนคอลัมน์คงที่ (`.req-tbl`) ให้ทุกใบตรงกัน
- มีเทสต์ **วัดพิกัดจริง**: ขอบซ้ายของทุกเซลล์ต้องตรงกับหัวคอลัมน์ของตัวเอง (คลาด ≤1px)
  ทั้ง 4 ตาราง — ไม่ใช่แค่เช็คว่ามี element อยู่
- **การ์ดรางวัลรายสาย (`.reward-grid`/`.rw-line`) เคยล้นกรอบ** — ช่อง grid กว้าง 221px
  แต่เนื้อใน (สาย + 3 เหรียญ + `฿700`) ต้องใช้ 241px ตัวเงินจึงโผล่ออกนอกเส้นขอบ
  แก้เป็น `minmax(250px,1fr)` + `flex-wrap` และบนมือถือเหลือคอลัมน์เดียว
  เทสต์วัด `scrollWidth > clientWidth` ทั้งแท็บที่ 390/768/1280/1600px (ข้ามกล่องที่ตั้งใจให้เลื่อน)

### ช่วงวันที่ของ "รอบเดียวกัน" — `REQ_BACK_DAYS` (บั๊กที่เจอ 17 ก.ย. 69)
รอบคำขอตัดที่ **9:00 น.** (`orderRoundDate`) เซลล์กดสั่ง 10 โมงวันที่ 14 → คำขอไปอยู่**รอบวันที่ 15**
แต่ศูนย์สั่งของไปแล้วเช้าวันที่ 14 → ใบสั่งจริงอยู่**วันที่ 14** สองอย่างนี้คนละวันกันโดยธรรมชาติ

**เดิมมี 3 ที่ที่ใช้ช่วงวันที่ไม่ตรงกัน ทำให้คำตอบขัดกันเอง:**
| ที่ | ช่วงเดิม | ผล |
|---|---|---|
| `reqFulfillment` (สั่งหรือยัง / ได้ของไหม) | `d >= reqDate` ข้างหน้าเท่านั้น | ไม่เห็นใบวันที่ 14 → "ศูนย์ไม่ได้สั่ง" |
| `ordRoundKnown` (อัพใบรอบนั้นแล้วไหม) | `x >= d` ข้างหน้าเท่านั้น | เหมือนกัน |
| `poRoundFor` (ใช้สีของใบรอบไหน) | ข้างหน้าก่อน ไม่มีก็ถอยไปใบล่าสุด ±7 วัน | **ถอยหลังได้** → เห็นใบวันที่ 14 |

`poRoundFor` เห็นใบวันที่ 14 แล้วบอกว่า "ช่องเปิดให้สั่ง" ส่วน `reqFulfillment` ไม่เคยมองใบนั้น
จึงรายงานว่าไม่ได้สั่ง — **ผู้ใช้เห็นสองคำตอบที่ขัดกัน**

**แก้โดยรวมเป็นค่าคงที่ตัวเดียว** `REQ_BACK_DAYS=1` + `reqFrom(reqDate)` แล้วให้ `reqFulfillment`
และ `ordRoundKnown` ใช้ตัวเดียวกัน · **ห้ามเขียน `d>=reqDate` กระจายไว้หลายที่อีก**
(`poRoundFor` ยังใช้ ±7 วันต่อไป ตั้งใจให้ต่าง เพราะ "สีบนใบ" เปลี่ยนช้ากว่า "สั่งไปหรือยัง")

**กระทบกว้างกว่าที่แจ้งมา** — แก้ที่เดียวแต่ซ่อมทั้ง: หน้าคำขอฝั่ง manager · หน้าคำขอฝั่งเซลล์ ·
รายงาน `soAgg` (เดิมนับ "ได้ของ" ขาด และนับ "ศูนย์ไม่สั่ง" เกิน ทุกครั้งที่ศูนย์สั่งวันก่อนหน้า)

**บั๊กตัวที่สองของตระกูลเดียวกัน (เจอ 17 ก.ย. 69): ใช้ "ใบผิดรอบ" มาตัดสิน**
ระบบสั่งของปกติของเจ้าของคือ **เซลล์ขอวันที่ 16 → ศูนย์เปิดใบสั่งวันที่ 17**
`poRoundFor` เดิมเลือก **ใบที่ตรงวันคำขอ** ก่อน (`if(ds.indexOf(reqDate)>=0)return reqDate`)
แต่สีบนใบเปลี่ยนระหว่างสองวันนั้นได้ — ของจริง: `65599222` ใบ 16 ยังเปิด แต่ใบ 17 ปิดช่อง (ฟ้า)
และ `65691965` ใบ 16 ปกติ แต่ใบ 17 คาดเทา → ระบบรายงาน "ศูนย์ไม่ได้สั่ง"
ทั้งที่ความจริงคือ "ยังไม่เปิดให้สั่ง" กับ "ของขาดชั่วคราว"

แก้โดยให้ `poRoundFor` เลือกตามลำดับ:
1. **ใบของรอบที่ศูนย์สั่งจริง** (`ordServedAt` = รอบล่าสุดในช่วง `reqFrom(reqDate)` ถึง `+PO_NEAR_DAYS`)
2. ใบที่ตรงวันคำขอ
3. ใบถัดไปข้างหน้า แล้วค่อยถอยหลัง (ยังบังคับ `PO_NEAR_DAYS` เหมือนเดิม)

`poWhy` คืน `served:true` เมื่อใช้ใบของรอบที่สั่งจริง ป้ายจึงขึ้น **"(ตามใบสั่ง 17 ก.ย.)"**
ไม่ใช่ "(อ้างอิงใบ …)" ซึ่งสงวนไว้สำหรับกรณีเดาจากใบรอบอื่นจริงๆ · `soAgg` นับ `approx`
เฉพาะกรณีหลังเท่านั้น
**ถ้ายังไม่ได้อัพใบสั่งรอบไหนเลย ต้องไม่โทษศูนย์** — ขึ้น "ยังไม่ได้อัพใบสั่งรอบนี้" ตามเดิม

**และบั๊กเงิน: PO อัตโนมัติเคยสั่งซ้ำของที่สั่งไปเมื่อวาน** ตอนนี้ `poBuild` หักช่อง `already`:
- `pending` (สั่งแล้วรอผล) → หักทุกกรณี ของกำลังมา
- ได้ของแล้ว แต่ **วันได้ของ >= วันที่ไฟล์สต็อก** → หัก
  **"วันเดียวกัน" ต้องหัก** (เดิมใช้ `>` ผิด): ไฟล์สต็อกวันเดียวกันไม่รู้ว่าถ่ายก่อนหรือหลังของเข้าคลัง
  เลือกฝั่ง "ยังไม่รวม" เพราะสั่งซ้ำเสียเงิน ส่วนสั่งขาดเพิ่มรอบหน้าได้
- ถูกดรอป → **ไม่หัก** ของไม่ได้มาจริง
ตัวเลขโชว์ในคอลัมน์ **สั่งไปแล้ว** + อธิบายในช่อง **ที่มา** ผู้จัดการกดแก้เองได้

### สร้างใบ PO จริง (`xlsmFill`) — ผ่าไฟล์เดิม ห้ามสร้างไฟล์ใหม่
วิเคราะห์จากใบจริง `..._Confirm_..._HPC_150926.xlsm` (677KB, 37 entry, 6 ชีต, 1,253 รหัส ไม่ซ้ำเลย)

- **ห้ามใช้ไลบรารี Excel เขียนไฟล์ใหม่** มาโคร (`xl/vbaProject.bin` 61,952 ไบต์) รูปแบบ สี เซลล์ผสาน
  การตั้งค่าพิมพ์ และ **`docMetadata/LabelInfo.xml` (ป้ายความปลอดภัยของ Unilever)** จะหายทั้งหมด
  วิธีที่ใช้: ไฟล์ .xlsm คือ zip → แก้เฉพาะ `xl/worksheets/sheetN.xml` ที่ต้องกรอก
  เขียนกลับด้วย **`{createFolders:false}`** ไม่งั้น JSZip แทรก entry โฟลเดอร์เพิ่มแล้วลำดับ zip เปลี่ยน
- **ช่องที่กรอก = `Order Confirm` = cc+7** (คอลัมน์ K / V / AG ในใบจริง — 3 บล็อคต่อชีต)
  หัวตาราง: แถวที่มี `รหัส` มี `Order` อยู่ด้วย แถวถัดไปมี `Confirm` · สินค้าเริ่มที่ `hdr+2`
- **⚠️ ตัวบล็อคที่เกือบพลาด: ใบจริงมีสูตร ~1,620 สูตรต่อชีตที่อ้างถึงช่อง Order Confirm**
  (`BJ11=IF(ISERROR(K11*1),0,K11*1)` จำนวน · `DD11=K11*DA11` น้ำหนัก · `DE11` ปริมาตร · `DF11` พาเลท ·
  `DJ11` ของแถม · `DR11` เช็คหารลงตัว · `B1=SUM(AQ11:AS100)` · `R1=SUM(BF11:BH100)*1.07` มูลค่า)
  ถ้าเติมเลขแล้วไม่สั่งคำนวณใหม่ **ผู้รับได้ใบที่มีจำนวนแต่น้ำหนัก/ปริมาตร/ของแถม/มูลค่าเป็น 0 ทั้งใบ**
  แก้โดยตั้ง **`fullCalcOnLoad="1"`** ใน `<calcPr>` (ตรวจว่าติดจริงก่อนสร้างไฟล์ ไม่ติด = โยน error)
  **และต้องบอกผู้ใช้ให้เปิดใน Excel หนึ่งครั้งก่อนส่งเมล** — เป็นขั้นที่ควรตรวจใบสั่งอยู่แล้ว
- **ไม่เก็บไฟล์ใบเปล่าไว้ในระบบ** — ให้เลือกไฟล์ตอนโหลด PO เพราะไฟล์ 700KB/รอบ จะดันข้อมูลก้อนหลัก
  เกินโควต้าคำขอของ Vercel และ `parseOrderForm` ทิ้ง buffer ไปแล้ว
- โหลด `public/vendor/jszip.min.js` (vendor ไว้ในรีโปเอง ไม่พึ่ง CDN) **แบบ lazy ตอนกดใช้จริง**
  ไม่ถ่วงการเปิดหน้าเว็บบนมือถือ
- กันพลาดที่ต้องคงไว้: พิกัดเกินขอบเขต Excel · ค่าไม่ใช่ตัวเลข · ชีตไม่มีในไฟล์ · **รหัสซ้ำในใบ** ·
  **เขียนทับเซลล์ที่เป็นสูตร** (ข้ามแล้วรายงาน) · เซลล์/แถวไม่มี `r=` · ไฟล์ใส่รหัสผ่าน
  → ทุกกรณีโยน error ภาษาไทยและ **ไม่สร้างไฟล์** ดีกว่าสร้างใบสั่งที่ผิดเงียบๆ
- ไฟล์จริงโชคดีหลายอย่าง (ตรวจแล้ว): ช่อง Confirm **มีอยู่แล้วพร้อมสี** `<c r="K11" s="429"/>`
  จึงไม่ต้องแทรกเซลล์ · `spans` เป็นช่วงเดียว `1:158` ไม่ใช่ลิสต์ · ไม่มีเซลล์ไร้ `r=` ·
  used range เริ่ม A1 พอดี (index ไม่เลื่อน) · ช่อง K/V/AG ไม่มีสูตร
  **ถ้าปีหน้า Unilever เปลี่ยนฟอร์ม ต้องตรวจทั้ง 5 ข้อนี้ใหม่**
- ชื่อไฟล์ออก `BD-PO-ddmmyy.xlsm` (ASCII — ดูกฎ `fnAscii`)
- เจ้าของเปิดไฟล์ที่ระบบสร้างใน Excel จริงแล้ว **ไม่ขึ้นเตือนซ่อมแซม ฟอร์มหน้าตาเหมือนเดิม** (15 ก.ย. 69)

### MINSTOCK — สต็อกขั้นต่ำที่ทยอยกรอก
`{man:{code:{v,at,by}}}` merge ด้วย **`newestById`** (ไม่ใช่ `keyMerge`)

- `keyMerge` ผิดสำหรับก้อนนี้: มันให้ทุก key ของ client ทับ แท็บที่เปิดค้างไว้ตั้งแต่เช้าซึ่งยังถือ v:5
  จะทับ v:9 ที่พิมพ์ตอนเที่ยง · `newestById` เทียบ `at` แล้วปฏิเสธการย้อนกลับ
- **ล้างค่าต้องเขียน `{v:null}` เป็น tombstone ห้ามลบ key** — merge ฝั่งเซิร์ฟเวอร์เป็นการรวม key
  ลบ key แล้วค่าเดิมจะกลับมาเองรอบหน้า (มีเทสต์พิสูจน์)
- **`v:0` เป็นค่าที่ตั้งใจได้** (= ห้ามสั่งอัตโนมัติ) จึงใช้ `null` แทนความหมาย "ยังไม่ตั้ง"
- **ค่าที่ระบบแนะนำไม่บันทึกลง `man`** — คิดสดตอนแสดงผล (`msSuggest`) ถ้าเขียนลงไปทั้งชุด
  ด้วย `at` ใหม่ จะทับค่าที่กรอกมือไว้ทั้งหมดตามกติกา newest-wins
- **บันทึกทีเดียวทั้งชุด ห้ามบันทึกทุกครั้งที่พิมพ์** — ทุกครั้งที่บันทึกจะเขียน snapshot เต็มก้อนแล้วหมุน
  backup (เก็บ 8 ชุด) กรอก 20 ตัวแบบบันทึกทีละช่อง = backup หายทั้งวง
- MINSTOCK **ไม่นับใน `looksEmpty`** เหมือน DCI เพราะ `looksEmpty` ใช้ทั้งขาเขียนและขาอ่าน
  (`newestReal`) ถ้านับ snapshot ที่มีแต่ MINSTOCK จะกลายเป็น "ของจริง" แล้วบังข้อมูลดีตัวก่อน

### KPI eB2B & Self Ordering — รอบข้อมูลเก็บเป็น "ณ วันที่" ไม่ใช่รายเดือน
ไฟล์ Salesman Incentive **อัพรายสัปดาห์** แต่เดิมเก็บด้วยคีย์รายเดือน (`"2026-09"`) เท่านั้น
ผลคือ **อัพรอบที่ 2 ของเดือน ทับรอบที่ 1 หายไปเลย** (เจอ 23 ก.ย. 69) และดูย้อนหลังไม่ได้

- คีย์รอบข้อมูลมาจาก `kpiKeyOf(name,hay)` ตามลำดับนี้ **อย่าสลับลำดับ — ชื่อไฟล์มาก่อนหัวรายงาน**
  (เจ้าของสั่งไว้ 23 ก.ย. 69: *"ใช่จากชื่อไฟล์เลยก็ได้นะ"* หลังหัวรายงานอ่านพลาดจริง):
  1. `kpiAsOf(name)` — "as of …" ใน **ชื่อไฟล์** เช่น `…_SEP (as of 23 Sep'26).xlsx`
  2. `kpiEndMonth(name)` — ชื่อไฟล์บอก `end of Sep` / `end Sep'26` / `EOM SEP 2026`
     = **รายงานรอบปิดเดือน** → คีย์เป็น **วันสุดท้ายของเดือนนั้น** (`kpiMonthEndDay`) และติดธง
     `close:true` ให้ป้ายขึ้นว่า "ปิดเดือน ก.ย. 2569 (30 ก.ย. 2569)" ไม่ใช่ "ณ …"
     กัน `(?<![a-z])end(?![a-z])` ไว้แล้ว ไม่งั้น `Trend Sep'26` / `weekend` จะติดด้วย
  3. `poDate6(name)` / `fnameDate(name)` — วันที่ล้วนๆ ในชื่อไฟล์ (`…_230926.xlsx`)
  4. `kpiAsOf(hay)` — "as of …" ในหัวรายงาน 6 แถวแรก **เป็นทางถอย เพราะเคยอ่านพลาด**
  5. `kpiMonthOf(name,hay)` (ตรรกะเดิม) แล้ว **ถ้าวันที่อัพอยู่ในเดือนนั้น → ใช้วันที่อัพ**
     ถ้าเป็นเดือนอื่น (อัพย้อนหลัง) → เก็บเป็นคีย์รายเดือนตามเดิม **ห้ามเดาวัน**
- `kpiAsOf` มีกับดัก 3 อันที่มีเทสต์กันไว้แล้ว **ห้ามคลายกฎ**:
  - **`"as of W4 Sep'26"` ไม่ใช่วันที่ 4** — มันคือสัปดาห์ที่ 4 นี่คือบั๊กจริงที่เจ้าของเจอ
    (ไฟล์รอบ 23 ก.ย. ขึ้นเป็น "ณ 4 ก.ย.") กันด้วย `weekish()` ที่ดูคำ `w` / `wk` / `week`
    ข้างหน้าตัวเลข — และต้องไม่ไปจับคำที่ลงท้ายด้วย w เช่น `now`
  - `"as of Sep 2026"` ต้องได้ `null` (ไม่ใช่วันที่ 20 — เลข 2026 ถูกหักเป็น 20+26) จึงบังคับให้มี
    ตัวคั่นระหว่างวันกับปี
  - ต้องเช็ค ISO (`2026-09-15`) **ก่อน** แบบ `15/09/2026` ไม่งั้นจะถูกอ่านกลับหัวเป็น
    26-09-15 → 2015-09-26
- `KPI.meta[key].raw` เก็บ**ข้อความที่จับมาได้** และแสดงบนแถบหัวแท็บในวงเล็บ — ครั้งหน้าถ้าอ่านวันผิด
  จะเห็นทันทีว่าไปจับอะไรมา ไม่ต้องเดา **อย่าเอาออก**
- **ลบรอบที่อ่านผิดได้** ปุ่ม 🗑 ลบรอบนี้ (manager เท่านั้น) → `kpiDeleteRound()` เขียน
  tombstone `KPI.del[key]=ts` **ห้ามลบ key เฉยๆ** เพราะเซิร์ฟเวอร์รวมคีย์แบบ union แท็บเก่าจะพาคืนมา
  `api/save.js` ใช้ tombstone **หลัง** การ union (แบบเดียวกับ `PSTORE.del`) และตัดคีย์นั้นออกจาก
  `months` ด้วย · อัพไฟล์รอบเดิมใหม่ชนะ tombstone เพราะ `meta.up` ใหม่กว่า (`parseKPI` ล้าง del ให้)
- **คีย์เก่าที่เป็นรายเดือน (`YYYY-MM`) ต้องใช้งานได้ต่อไป** — อยู่ใน `KPI.data` ก้อนเดียวกัน
  ช่องเลือกแยกเป็น 2 optgroup ("ณ วันที่" / "ข้อมูลเก่า (รายเดือน)") ใหม่สุดอยู่บนสุด
- `remainWorkdays(k)` **ต้องเรียก `kpiMonthKey(k)` ก่อนเทียบ** เพราะ `"2026-09-15" > "2026-09"`
  เป็นจริง ถ้าไม่ตัดจะตอบว่าเหลือวันทำงานเต็มเดือนทั้งที่เป็นเดือนที่ผ่านมาแล้ว
- `kpiKeys()` เป็นที่เดียวที่รวมคีย์จาก `KPI.months` + `Object.keys(KPI.data)` ใช้ทั้ง `initKPI`
  และ `renderKPI` ไม่ให้ตัวเลือกกับหน้าจอไม่ตรงกัน
- `KPI.meta[key] = {up,file,src}` จำว่ารอบนั้นมาจากไฟล์ไหนและวันที่ได้มาจากไหน แสดงบนแถบหัวแท็บ
  **merge ใน `api/save.js` ต้องหอบ `meta` ไปด้วย** (`keyMerge`) ไม่งั้นทุกครั้งที่บันทึกจะหายเงียบ —
  TEST 16 และ TEST 17 ใน `test/merge-safety.test.mjs` กันเรื่องนี้และเรื่อง tombstone ไว้

### ชื่อไฟล์ดาวน์โหลดต้องเป็น ASCII เท่านั้น (`fnAscii`)
**Chromium ทิ้งชื่อไฟล์ทั้งชื่อ รวมทั้งนามสกุล ถ้า `a.download` มีอักษรไทยแม้ตัวเดียว** แล้วเซฟออกมา
เป็นไฟล์ชื่อ `download` เฉยๆ เปิดด้วย Excel ไม่ได้ (พิสูจน์แล้วกับ Chromium 140 ทั้งบน http และ data: URL)
ทุกปุ่มดาวน์โหลดจึงต้องตั้งชื่อเป็นอังกฤษ/ตัวเลขล้วนผ่าน `fnAscii` — ชื่อร้านภาษาไทยอยู่**ข้างใน**ไฟล์แล้ว
(`BD-shop-0814919825.csv` · `BD-shop-0814919825.jpg` · `BD-newshops-CT13-2026-09-11.csv` · `BD-leads-….csv`)

### เซลล์ส่งร้านใหม่ต่อให้แอดมินเปิดบัญชี (แท็บ 🏪 ร้านใหม่ของฉัน)
ในการ์ดของแต่ละร้านมี 3 ปุ่มใต้หัวข้อ "ส่งต่อให้แอดมินเปิดบัญชีลูกค้าใหม่":
คัดลอกข้อความ (`leadCopyOne` — ตัวเดียวกับฝั่ง manager) · เซฟข้อมูล CSV (`msSaveOne`) ·
เซฟรูปหน้าร้าน (`msSavePhoto`) และมี `msSaveAll` เซฟทุกร้านในสายทีเดียวที่หัวรายการ

- รูปอยู่บนโดเมนของ Vercel Blob (คนละโดเมนกับเว็บ) ถ้า `fetch` ไม่ผ่าน (CORS/เน็ตล่ม)
  `msSavePhoto` จะ**เปิดรูปในแท็บใหม่**ให้กดค้างเซฟเอง + ขึ้น toast บอกวิธี — ห้ามให้กดแล้วเงียบ
- ปุ่มเซฟรูปขึ้นเฉพาะร้านที่แนบรูปมา ร้านที่ไม่มีรูปขึ้นปุ่ม disabled ว่า "ไม่มีรูปหน้าร้าน"
- `MS_CSV_HEAD` คือคอลัมน์ที่แอดมินต้องใช้เปิดบัญชี ถ้าเพิ่มฟิลด์ในใบสมัคร (`public/join.html`)
  ต้องเพิ่มที่นี่ด้วย ไม่งั้นข้อมูลใหม่จะไม่ไปถึงแอดมิน

## 6. ประวัติการแก้ไข (เรียงตามที่ทำ)

**ช่วงสร้างระบบ**
- สร้าง dashboard จาก Excel → ยอดขาย/เป้า/KPI → drill-down รายร้าน → ปุ่มอัพโหลด
- ทำ backend (login/data/save) + auth → ย้ายจากไฟล์เดียวมาเป็นเซิร์ฟเวอร์กลาง Vercel + Blob
- KPI tab (Active/Self/Ghost) + เงินรางวัล + สีตาม Excel
- แท็บการสั่งของ + แท็บสต็อก (คอลัมน์, สลับคลังขาย-แถม, ค้นหา, กรองของหมด/ติดลบ)
- ฟีเจอร์คำขอสินค้า: เซลล์คีย์ → manager สรุป → สถานะได้/ไม่ได้ของ
- แท็บวิเคราะห์เชิงลึก: parser ไฟล์ใหญ่ (MHTML ~60MB), pie คลิกกรองได้, Top 20, เทียบเวลา, A/B
- เจาะสินค้ารายตัวในแต่ละร้าน (STOREPROD) + ค้นหาร้าน + ปุ่ม back ทุกหน้า

**ช่วงแก้ปัญหาใหญ่**
- **ข้อมูลหาย** → วาง `mergeState` / `mergeRequests` / กันเซฟว่าง / backup 8 ชุด / อ่าน snapshot ไม่ว่าง
- **HTTP 413 บันทึกไม่สำเร็จ** → gzip payload (8.78MB → 1.23MB)
- **สถานะ "ไม่มีในใบสั่ง" ผิด** (แป้งข้าวโพด) → เพิ่มจับคู่ด้วยชื่อสินค้าเมื่อรหัสเปลี่ยน

**ช่วงล่าสุด**
- สต็อก: เพิ่มคอลัมน์ ขนาดบรรจุ / ต้นทุนต่อหีบ / ต้นทุนรวม (manager เท่านั้น) + "เปิดใบสั่งซื้อ" (ทุกคนเห็น)
- ปุ่มค้นหาสถานะสินค้าข้ามวัน ทั้งฝั่งเซลล์และ manager
- จัดกลุ่มคำขอรวมตาม 6 หมวดใบสั่งซื้อ (จับได้ 1,248 รหัส + 1,162 ชื่อ)
- การ์ด CON Confirm 4 บรรทัด + เก็บ Drop Value + เอา "ยอดประมาณ" ออก
- ปุ่มสลับตาราง Drop: สรุปยอดรวม / แบบ Excel
- เตรียม repo สำหรับ Claude Code: เพิ่ม `dashboard_template.html`, `build.cjs`, `CLAUDE.md`
- KPI eB2B & Self Ordering: เลือกดูเป็น **"ณ วันที่"** ได้ (เดิมเลือกได้แค่รายเดือน) และแก้บั๊กที่
  อัพไฟล์รอบที่ 2 ของเดือนเดียวกันทับรอบแรกหายไป — รายละเอียดใน §5

---

## 7. ข้อจำกัดที่ต้องรู้

- **วันเก่าที่อัพ Drop ไว้ก่อนหน้านี้** จะยังไม่มีมูลค่า Drop และมุมมอง "แบบ Excel"
  (เพิ่งเริ่มเก็บข้อมูลส่วนนี้) → **อัพไฟล์ Drop ของวันนั้นซ้ำอีกครั้ง** แล้วจะครบ ปลอดภัยกับข้อมูลเดิม
- **ยอดขายปีก่อนยังไม่ได้อัพ** — ระบบเทียบรายปีเตรียมไว้แล้ว รอข้อมูล
- **Hybrid manual grouping** (ให้ผู้ใช้ override การจัดกลุ่มเอง) — ยังไม่ได้ทำ

---

## 8. Vercel (Pro $20/เดือน)

เคยล่มเพราะแพ็คฟรีจำกัด Blob 10GB/เดือน ตอนนี้ Pro ได้ 1TB + เครดิต $20
ใช้จริงประมาณ **$3/เดือน = ~15% ของเครดิตที่แถม** → เหลือที่โตอีกหลายปี

**ควรทำ:** ตั้ง Spend limit (~$30) + เปิด Usage Alert 50/75/100% ที่ Settings → Billing
**ยังไม่ทำ (ข้อเสนอ):** แยกข้อมูลหน้าวิเคราะห์ (STOREPROD ~6.6MB) ให้โหลดเฉพาะตอนเปิดแท็บ
→ ก้อนหลักจะเหลือ ~2MB ทุกครั้งที่เซลล์สั่งของ (ลดโหลด 4–5 เท่า แก้ต้นเหตุการล่มโดยตรง)

---

## 9. สไตล์การทำงานที่ตกลงกันไว้

- ตอบเป็น **ภาษาไทย** เรียบง่าย ไม่ใช้ศัพท์เทคนิคเกินจำเป็น
- บอกก่อนทำสิ่งที่ย้อนไม่ได้ (commit / push / deploy)
- แก้ทีละน้อย ทดสอบก่อน push — โดยเฉพาะอะไรที่แตะข้อมูล
- เก็บไฟล์เวอร์ชันเดียว ไม่สะสมไฟล์เก่าให้รกโฟลเดอร์
